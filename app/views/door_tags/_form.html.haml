= form_for @door_tag do |f|
  - if @door_tag.errors.any?
    #error_explanation
      %h2= "#{pluralize(@door_tag.errors.count, "error")} prohibited this door_tag from being saved:"
      %ul
        - @door_tag.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :tag
    = f.text_field :tag
  .actions
    = f.submit 'Save'
