%h1 Listing destroys

%table
  %thead
    %tr
      %th Comment
      %th
      %th
      %th

  %tbody
    - @destroys.each do |destroy|
      %tr
        %td= destroy.comment
        %td= link_to 'Show', destroy
        %td= link_to 'Edit', edit_destroy_path(destroy)
        %td= link_to 'Destroy', destroy, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Destroy', new_destroy_path
