= form_for @destroy do |f|
  - if @destroy.errors.any?
    #error_explanation
      %h2= "#{pluralize(@destroy.errors.count, "error")} prohibited this destroy from being saved:"
      %ul
        - @destroy.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :comment
    = f.text_field :comment
  .actions
    = f.submit 'Save'
